CONFIG_BUILD_OUTPUT_HEX=y

# Make the artifacts we need for OpenOCD JTAG flashing
CONFIG_BUILD_OUTPUT_BIN=y         # keep HEX too if you want: CONFIG_BUILD_OUTPUT_HEX=y


# Use UART0 instead of USB CDC for console
CONFIG_UART_CONSOLE=y
#CONFIG_USB_DEVICE_STACK=n
#CONFIG_SERIAL=y
#CONFIG_CONSOLE=y
#CONFIG_RTT_CONSOLE=n


